import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shreeva.urbanlaundrydelivery',
  appName: 'urban-laundry-delivery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
